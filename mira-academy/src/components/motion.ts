'use client';

// Import individual exports from framer-motion
import {
  motion,
  AnimatePresence,
  useScroll,
  useTransform,
  useAnimation,
  useInView,
  useMotionValue,
  useSpring,
  MotionConfig
} from 'framer-motion';

// Re-export them individually (not using wildcard exports)
export {
  motion,
  AnimatePresence,
  useScroll,
  useTransform,
  useAnimation,
  useInView,
  useMotionValue,
  useSpring,
  MotionConfig
}; 